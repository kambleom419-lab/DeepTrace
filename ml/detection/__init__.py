"""DeepTrace ML detection package.

Run from the ml/ directory (so `detection` is importable):

    python -m detection.pipeline  sample.mp4            # analyze a video
    python -m detection.train     --stage spatial       # train a head
    python -m detection.evaluate  --dataset-root DIR    # metrics
"""
